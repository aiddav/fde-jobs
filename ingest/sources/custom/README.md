# Custom Adapters

Add per-company HTML or JSON adapters here only when no public ATS API exists. Custom adapters should export a `SourceAdapter` and respect robots.txt, throttling, and the project User-Agent.
